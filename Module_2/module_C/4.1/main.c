#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "header.h"

int main()
{
    Node *Head = NULL;
    Menu(&Head);
    // Directory temp1 = {1, "fsdsd", "dfgfd", {1, 2, 3}};

    // Directory temp2 = {3, "Alice", "Smith", {4, 5, 2023}};

    // Directory temp3 = {4, "Fil", "Smith", {4, 5, 2023}};

    // Directory temp4 = {8, "Mars", "Smith", {4, 5, 2023}};

    // Directory temp5 = {6, "Spectra", "Smith", {4, 5, 2023}};

    // Directory temp6 = {100, "Spectra", "Smith", {4, 5, 2023}};

    // Directory temp7 = {0, "Spectra", "Smith", {4, 5, 2023}};

    // Directory temp8 = {0, "Axe", "mint", {5, 6, 2723}};

    // Node *Head = NULL;

    // AddinHead(&Head, &temp1);
    // AddinHead(&Head, &temp2);
    // AddinHead(&Head, &temp3);
    // AddinHead(&Head, &temp4);
    // AddinSort(&Head, &temp5);
    // AddinSort(&Head, &temp6);
    // AddinSort(&Head, &temp1);
    // AddinSort(&Head, &temp7);
    // AddinSort(&Head, &temp3);

    // PrintList(Head);

    // DeleteNode(&Head, 6);
    // DeleteNode(&Head, 4);

    // PrintList(Head);

    // EditNode(&Head, &temp8, 3);

    // PrintList(Head);
    return 0;
}