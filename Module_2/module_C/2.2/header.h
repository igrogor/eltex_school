#ifndef HEADER_H_
#define HEADER_H_
#include <stdio.h>
#include <stdlib.h>
#include <stdarg.h>


void menu();

int addition(int size, ...);

int subtraction(int size, ...);

int multiplication(int size, ...);

float division(int size, ...);
#endif
