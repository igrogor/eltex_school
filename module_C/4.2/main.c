#include "header.h"
#include <stdio.h>
#include <string.h>

int main()
{

    queue *head = NULL;
    // generate_messages(&head, 200);

    // printf("=====================\n");

    // printqueue(head);

    // printf("=====================\n");

    // dequeue(&head, 45);
    // dequeue(&head, 44);
    // dequeue(&head, 43);
    // dequeue(&head, 42);


    // printqueue(head);

    // printf("=====================\n");

    // free_queue(&head);

    // printqueue(head);

    menu(&head);

    return 0;
}
